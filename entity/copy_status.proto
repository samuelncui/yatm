syntax = "proto3";
package copy_status;

option go_package = "github.com/samuelncui/tapewriter/entity";

enum CopyStatus {
    DRAFT     = 0;
    PENDING   = 1; // waiting in queue
    RUNNING   = 2;
    STAGED    = 3;
    SUBMITED  = 4;

    FAILED  = 255;
}
