// Code generated by entity/gen/main.go. DO NOT EDIT.

import {
	FileSelection,
	LibrarySelection,
	LocationSelection,
} from ".";

export function unpackFileSelection(wrapper: FileSelection): LibrarySelection | LocationSelection | undefined {
	if (!wrapper.target) return undefined;
	switch (wrapper.target.oneofKind) {
		case "library":
			return wrapper.target.library;
		case "location":
			return wrapper.target.location;
		default:
			return undefined;
	}
}

export function packLibrarySelection(param: LibrarySelection): FileSelection {
	return FileSelection.create({
		target: {
			oneofKind: "library",
			library: param,
		}
	});
}

export function packLocationSelection(param: LocationSelection): FileSelection {
	return FileSelection.create({
		target: {
			oneofKind: "location",
			location: param,
		}
	});
}
